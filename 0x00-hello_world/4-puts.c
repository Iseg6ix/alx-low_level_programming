#include <stdio.h>
/**
 *Everything is done in the main function
 *It returns a value of zero if no errors is encountered, else it returns a non zero value
*/

int main(){
	puts("Programming is like building a multilingual puzzle");
	return (0);
}	
