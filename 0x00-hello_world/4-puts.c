#include <stdio.h>
/**
 *main - Serves as the entry point of the code. Everything is done in the main function.
 *Return: It returns a value of zero if no errors is encountered, else it returns a non zero value.
*/

int main()
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}	
