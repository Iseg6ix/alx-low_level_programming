#include <string.h>
#include "main.h"
/**
 * _strlen - This function returns the length of a string.
 * @s: Pointer argument s.
 *
 * Return: Lenght of string.
 */
int _strlen(char *s)
{
	return(strlen(*s));
}
