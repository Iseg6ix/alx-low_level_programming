#include "main.h"
/**
 * malloc_checked - This function doess something.
 * @b: First argument.
 *
 * Return: NULL.
 */
void *malloc_checked(unsigned int b);
{
	int *p;

	*p = malloc(sizeof(b));
	if (p = NULL)
		return (exit(98));
	return *p;
}
